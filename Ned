repeat task.wait() until game:IsLoaded()

if getgenv().AntiAFKActive then
    if game.CoreGui:FindFirstChild("AntiAFKGui") then
        game.CoreGui.AntiAFKGui:Destroy()
    end
end

getgenv().AntiAFKActive = true

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local VirtualUser = game:GetService("VirtualUser")

local Player = Players.LocalPlayer

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AntiAFKGui"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

local Main = Instance.new("Frame")
Main.Parent = ScreenGui
Main.Size = UDim2.new(0,260,0,160)
Main.Position = UDim2.new(0.02,0,0.3,0)
Main.BackgroundColor3 = Color3.fromRGB(18,18,18)
Main.BorderSizePixel = 0

local TopBar = Instance.new("Frame")
TopBar.Parent = Main
TopBar.Size = UDim2.new(1,0,0,32)
TopBar.BackgroundColor3 = Color3.fromRGB(24,24,24)
TopBar.BorderSizePixel = 0

local Title = Instance.new("TextLabel")
Title.Parent = TopBar
Title.Text = "L.M.P | ANTI-AFK"
Title.Size = UDim2.new(1,-60,1,0)
Title.Position = UDim2.new(0,10,0,0)
Title.TextColor3 = Color3.new(1,1,1)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.SourceSans
Title.TextSize = 14
Title.TextXAlignment = Enum.TextXAlignment.Left

local Close = Instance.new("TextButton")
Close.Parent = TopBar
Close.Size = UDim2.new(0,32,0,32)
Close.Position = UDim2.new(1,-32,0,0)
Close.Text = "X"
Close.BackgroundTransparency = 1
Close.TextColor3 = Color3.new(1,1,1)

local Minimize = Instance.new("TextButton")
Minimize.Parent = TopBar
Minimize.Size = UDim2.new(0,32,0,32)
Minimize.Position = UDim2.new(1,-64,0,0)
Minimize.Text = "□"
Minimize.BackgroundTransparency = 1
Minimize.TextColor3 = Color3.new(1,1,1)

Close.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- ANTI AFK

Player.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

-- BOTON FLOTANTE

local FloatButton = Instance.new("Frame")
FloatButton.Parent = ScreenGui
FloatButton.Size = UDim2.new(0,60,0,60)
FloatButton.Position = UDim2.new(0.1,0,0.5,0)
FloatButton.Visible = false
FloatButton.BorderSizePixel = 0
FloatButton.BackgroundTransparency = 1

local Left = Instance.new("Frame")
Left.Parent = FloatButton
Left.Size = UDim2.new(0.5,0,1,0)
Left.BackgroundColor3 = Color3.fromRGB(255,0,0)
Left.BorderSizePixel = 0

local Right = Instance.new("Frame")
Right.Parent = FloatButton
Right.Size = UDim2.new(0.5,0,1,0)
Right.Position = UDim2.new(0.5,0,0,0)
Right.BackgroundColor3 = Color3.fromRGB(0,120,255)
Right.BorderSizePixel = 0

local Stroke = Instance.new("UIStroke")
Stroke.Parent = FloatButton
Stroke.Thickness = 1
Stroke.Color = Color3.new(1,1,1)

-- ICONOS

local Icon1 = Instance.new("ImageLabel")
Icon1.Parent = FloatButton
Icon1.BackgroundTransparency = 1
Icon1.Size = UDim2.new(0,22,0,22)
Icon1.Position = UDim2.new(0.25,-11,0.5,-11)
Icon1.Image = "rbxassetid://74954519069199"

local Icon2 = Instance.new("ImageLabel")
Icon2.Parent = FloatButton
Icon2.BackgroundTransparency = 1
Icon2.Size = UDim2.new(0,22,0,22)
Icon2.Position = UDim2.new(0.75,-11,0.5,-11)
Icon2.Image = "rbxassetid://108256864313822"

-- ANIMACION BORDE

task.spawn(function()
    while true do
        TweenService:Create(
            Stroke,
            TweenInfo.new(1),
            {Transparency = 0.7}
        ):Play()

        task.wait(1)

        TweenService:Create(
            Stroke,
            TweenInfo.new(1),
            {Transparency = 0}
        ):Play()

        task.wait(1)
    end
end)

-- MINIMIZAR

Minimize.MouseButton1Click:Connect(function()
    Main.Visible = false
    FloatButton.Visible = true
end)

-- DOBLE CLICK

local LastClick = 0

FloatButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        local t = tick()

        if t - LastClick < 0.35 then
            Main.Visible = true
            FloatButton.Visible = false
        end

        LastClick = t
    end
end)

-- DRAG BOTON

local dragging = false
local dragInput
local dragStart
local startPos

FloatButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = FloatButton.Position
    end
end)

FloatButton.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart

        FloatButton.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

UIS.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)
