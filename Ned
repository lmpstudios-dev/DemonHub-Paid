-- ts file was generated at discord.gg/25ms

repeat
    task.wait()
until game:IsLoaded() and (game.Players and (game.Players.LocalPlayer and game.Players.LocalPlayer.Character))

if getgenv().AntiAFKActive then
    if game.CoreGui:FindFirstChild('AntiAFKGui') then
        game.CoreGui.AntiAFKGui:Destroy()
    end
    getgenv().AntiAFKActive = false
    getgenv().TimerRunning = false
    task.wait(0.5)
end

getgenv().AntiAFKActive = true

local _Players = game:GetService('Players')
local _CoreGui = game:GetService('CoreGui')
local _TweenService = game:GetService('TweenService')
local _RunService = game:GetService('RunService')
local _UserInputService = game:GetService('UserInputService')
local _VirtualUser = game:GetService('VirtualUser')
local _Stats = game:GetService('Stats')
local _LocalPlayer = _Players.LocalPlayer

local u9 = {
    UpdateInterval = 1,
    FPSUpdateRate = 0.1,
    DragSmoothness = 0.04,
}

-- Función de arrastre (Integrada para ambos elementos)
local function makeDraggable(obj)
    local dragging, dragInput, dragStart, startPos
    obj.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = obj.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    obj.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
    end)
    _UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            _TweenService:Create(obj, TweenInfo.new(u9.DragSmoothness), {
                Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
            }):Play()
        end
    end)
end

-- CONSTRUCCIÓN DE LA UI COMPLETA
local u30, v31, u32, u33, u34, u35, u36 = (function()
    local _ScreenGui = Instance.new('ScreenGui', _CoreGui)
    _ScreenGui.Name = 'AntiAFKGui'
    _ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    -- FRAME PRINCIPAL
    local _MainFrame = Instance.new('Frame', _ScreenGui)
    _MainFrame.Name = 'MainFrame'
    _MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    _MainFrame.Position = UDim2.new(0.02, 0, 0.3, 0)
    _MainFrame.Size = UDim2.new(0, 260, 0, 160)
    _MainFrame.Active = true
    Instance.new('UICorner', _MainFrame).CornerRadius = UDim.new(0, 4)
    local _MainStroke = Instance.new('UIStroke', _MainFrame)
    _MainStroke.Color = Color3.fromRGB(40, 40, 40)

    -- BARRA SUPERIOR
    local _TopBar = Instance.new('Frame', _MainFrame)
    _TopBar.Size = UDim2.new(1, 0, 0, 32)
    _TopBar.BackgroundColor3 = Color3.fromRGB(24, 24, 24)

    local _Title = Instance.new('TextLabel', _TopBar)
    _Title.Text = 'L.M.P | ANTI-AFK'
    _Title.TextColor3 = Color3.fromRGB(220, 220, 220)
    _Title.Size = UDim2.new(0.5, 0, 1, 0)
    _Title.Position = UDim2.new(0, 12, 0, 0)
    _Title.BackgroundTransparency = 1
    _Title.TextXAlignment = Enum.TextXAlignment.Left
    _Title.Font = Enum.Font.SourceSans

    -- BOTÓN CERRAR "X"
    local _CloseBtn = Instance.new('TextButton', _TopBar)
    _CloseBtn.Text = 'X'
    _CloseBtn.Size = UDim2.new(0, 32, 0, 32)
    _CloseBtn.Position = UDim2.new(1, -32, 0, 0)
    _CloseBtn.BackgroundTransparency = 1
    _CloseBtn.TextColor3 = Color3.fromRGB(180, 180, 180)

    -- BOTÓN MINIMIZAR "□" (Corregido)
    local _MinBtn = Instance.new('TextButton', _TopBar)
    _MinBtn.Name = 'MinimizeBtn'
    _MinBtn.Text = '□'
    _MinBtn.Size = UDim2.new(0, 32, 0, 32)
    _MinBtn.Position = UDim2.new(1, -64, 0, 0)
    _MinBtn.BackgroundTransparency = 1
    _MinBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
    _MinBtn.TextSize = 20

    -- STATS (FPS, PING, UPTIME - Originales)
    local _StatsFrame = Instance.new('Frame', _MainFrame)
    _StatsFrame.Position = UDim2.new(0, 12, 0, 45)
    _StatsFrame.Size = UDim2.new(1, -24, 0, 75)
    _StatsFrame.BackgroundTransparency = 1

    local function stat(name, y)
        local l = Instance.new('TextLabel', _StatsFrame); l.Text = name; l.Position = UDim2.new(0,0,0,y); l.Size = UDim2.new(0,80,0,20); l.TextColor3 = Color3.fromRGB(140,140,140); l.BackgroundTransparency = 1; l.TextXAlignment = Enum.TextXAlignment.Left
        local v = Instance.new('TextLabel', _StatsFrame); v.Text = '0'; v.Position = UDim2.new(0,80,0,y); v.Size = UDim2.new(1,-80,0,20); v.TextColor3 = Color3.fromRGB(240,240,240); v.BackgroundTransparency = 1; v.TextXAlignment = Enum.TextXAlignment.Right
        return v
    end

    local _TimerValue = stat('UPTIME', 0)
    local _FPSValue = stat('FPS', 25)
    local _PingValue = stat('PING', 50)

    local _Indicator = Instance.new('Frame', _MainFrame)
    _Indicator.Size = UDim2.new(0, 6, 0, 6)
    _Indicator.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
    _Indicator.Position = UDim2.new(0, 18, 1, -22)
    Instance.new('UICorner', _Indicator).CornerRadius = UDim.new(1, 0)

    -- BOTÓN FLOTANTE (Corrección de tamaño y bordes)
    local _FloatBtn = Instance.new('Frame', _ScreenGui)
    _FloatBtn.Name = 'FloatingButton'
    _FloatBtn.Size = UDim2.new(0, 45, 0, 45) -- Tamaño reducido
    _FloatBtn.Position = UDim2.new(0.5, -22, 0.1, 0)
    _FloatBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    _FloatBtn.Visible = false
    _FloatBtn.Active = true
    _FloatBtn.BorderSizePixel = 0 -- Sin bordes automáticos para usar UIStroke

    local _FStroke = Instance.new('UIStroke', _FloatBtn)
    _FStroke.Thickness = 1 -- Borde muy delgado
    _FStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    local _FGradient = Instance.new('UIGradient', _FStroke)
    _FGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)), -- Rojo
        ColorSequenceKeypoint.new(0.45, Color3.fromRGB(255, 0, 0)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)), -- Luz Blanca
        ColorSequenceKeypoint.new(0.55, Color3.fromRGB(0, 0, 255)), -- Azul
        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 255))
    })

    -- ASSETS CORREGIDOS (Lado a lado, sin salirse)
    local _AssetContainer = Instance.new('Frame', _FloatBtn)
    _AssetContainer.Size = UDim2.new(0.8, 0, 0.6, 0)
    _AssetContainer.Position = UDim2.new(0.1, 0, 0.2, 0)
    _AssetContainer.BackgroundTransparency = 1

    local _Img1 = Instance.new('ImageLabel', _AssetContainer)
    _Img1.Size = UDim2.new(0.5, -2, 1, 0)
    _Img1.Image = 'rbxassetid://74954519069199'
    _Img1.BackgroundTransparency = 1
    _Img1.ScaleType = Enum.ScaleType.Fit

    local _Img2 = Instance.new('ImageLabel', _AssetContainer)
    _Img2.Size = UDim2.new(0.5, -2, 1, 0)
    _Img2.Position = UDim2.new(0.5, 2, 0, 0)
    _Img2.Image = 'rbxassetid://108256864313822'
    _Img2.BackgroundTransparency = 1
    _Img2.ScaleType = Enum.ScaleType.Fit

    -- Lógica de Intercambio
    _MinBtn.MouseButton1Click:Connect(function()
        _MainFrame.Visible = false
        _FloatBtn.Visible = true
    end)

    local lastClick = 0
    _FloatBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            local now = tick()
            if now - lastClick < 0.3 then
                _MainFrame.Visible = true
                _FloatBtn.Visible = false
            end
            lastClick = now
        end
    end)

    -- Animación de luz blanca giratoria
    task.spawn(function()
        while true do
            _FGradient.Rotation = (_FGradient.Rotation + 4) % 360
            task.wait(0.02)
        end
    end)

    makeDraggable(_MainFrame)
    makeDraggable(_FloatBtn)

    return _ScreenGui, _MainFrame, _CloseBtn, _TimerValue, _FPSValue, _PingValue, _Indicator
end)()

-- LÓGICA DE RENDIMIENTO (Manteniendo intacto tu archivo original)
_LocalPlayer.Idled:Connect(function()
    _VirtualUser:CaptureController()
    _VirtualUser:ClickButton2(Vector2.new())
    _TweenService:Create(u36, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
    task.wait(0.15)
    _TweenService:Create(u36, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(46, 204, 113)}):Play()
end)

local u49 = {}
local u50 = 0
_RunService.RenderStepped:Connect(function()
    local v51 = tick()
    for v52 = #u49, 1, -1 do
        if u49[v52] < v51 - 1 then table.remove(u49, v52) end
    end
    table.insert(u49, v51)
    if v51 - u50 >= u9.FPSUpdateRate then
        u34.Text = tostring(#u49)
        u50 = v51
    end
end)

task.spawn(function()
    while getgenv().AntiAFKActive do
        local v54, v55 = pcall(function() return _Stats.PerformanceStats.Ping:GetValue() end)
        u35.Text = v54 and (math.floor(v55) .. ' ms') or 'N/A'
        task.wait(u9.UpdateInterval)
    end
end)

local u56, u57, u58 = 0, 0, 0
getgenv().TimerRunning = true
task.spawn(function()
    while getgenv().TimerRunning do
        task.wait(1)
        if getgenv().AntiAFKActive then
            u56 = u56 + 1
            if u56 >= 60 then u56 = 0; u57 = u57 + 1 end
            if u57 >= 60 then u57 = 0; u58 = u58 + 1 end
            u33.Text = string.format('%02d:%02d:%02d', u58, u57, u56)
        end
    end
end)

u32.MouseButton1Click:Connect(function()
    getgenv().AntiAFKActive = false
    getgenv().TimerRunning = false
    u30:Destroy()
end)

game.StarterGui:SetCore('SendNotification', {
    Title = 'L.M.P | ANTI-AFK',
    Text = 'Script Corregido y Completo',
    Duration = 3,
})
